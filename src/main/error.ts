enum ErrorType {
  CONNECTION,
  EXTERN,
  SDPP,
  BALLOT_ACK,
  BALLOT_ACK_FAIL,
  DECRYPT,
  FORMAT,
  ZKP_REJECT,
  ZKP_INV,
  INVALID_OPERATION,
  DECODE,
  PARAMS,
  OTHER
}

export { ErrorType }
